using AttendanceManagement;
using SalaryManagement;

namespace FirmManagementApplication
{
    internal class Testing
    {
        static void Main()
        {
            ////testing attendance of an employee
            //DayWorkType wt = DayWorkType.FullDay;
            //Attendance attendance = new Attendance(102, wt);

            //// testing leave of an employee
            //DateOnly leaveDate = DateOnly.FromDateTime(DateTime.Parse("04-11-2023"));
            //LeaveType leaveType = LeaveType.Emergency;
            //Leave leave = new Leave(102, leaveDate, "Emergency leave", leaveType);

            //// testing the credit salary
            //Salary.CreditAllEmployeesSalary();
        }
    }
}
